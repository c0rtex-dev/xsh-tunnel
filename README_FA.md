# 🚀 نصب سریع تانل SSH [XSH Tunnel] 

یک اسکریپت ساده و قابل اعتماد برای راه‌اندازی سریع تانل SSH امن بین سرورهای شما. مناسب کاربران نهایی، این اسکریپت تنظیم کلید SSH، فوروارد چند پورت و ایجاد سرویس systemd را خودکار می‌کند.

---

## ویژگی‌ها

- تانل SSH پایدار با ری‌استارت خودکار در صورت قطع شدن
- فوروارد چند پورت
- نصب و حذف آسان
- نیاز به دانش فنی ندارد

---

## پیش‌نیازها

- سرور لینوکس (توصیه: Debian/Ubuntu)
- دسترسی روت
- اتصال اینترنت برای نصب اولیه
- برنامه‌های `ssh`, `ssh-keygen`, `ssh-copy-id`, `autossh`, `systemctl` (اسکریپت در صورت نبودن آنها را نصب می‌کند)

---

## نصب

1. دانلود و نصب آسان  :

```bash
bash <(curl -Ls https://raw.githubusercontent.com/c0rtex-dev/xsh-tunnel/main/xsh-tunnel.sh)
```

## جهت نصب تانل موارد خواسته شده ذیل را به ترتیب وارد کنید : 

- IP و Port سرور خارج
- نام کاربر سرور خارجی با دسترسی root
- IP سرور ایران جهت LISTEN کردن ترافیک ورودی (معمولا `0.0.0.0`)
- نام سرور یا آی پی مقصد (معمولا `localhost`)
- پورت های ورودی و خروجی  (با قابلیت پشتیبانی از چند پورت)

> **_NOTE:_**  میتوانید از پورت های متفاوت برای ورودی و خروجی استفاده نمایید

اسکریپت این کار ها را انجام می‌دهد: 

- ساخت کلید SSH
- انتقال کلید به سرور راه دور
- ایجاد سرویس systemd برای حفظ تانل
- اعمال تغییرات سیستم برای دسترسی به پورت‌های پایین

## نحوه استفاده 

###  بررسی وضعیت تانل: 

```bash
systemctl status iran-to-foreign-xsh-tunnel
```

### ری‌استارت دستی تانل: 

```bash
systemctl restart iran-to-foreign-xsh-tunnel
```

### توقف تانل: 

```bash
systemctl stop iran-to-foreign-xsh-tunnel
```

## حذف 

### برای حذف کامل همه چیز از دستور زیر استفده نمایید: 

```bash
bash <(curl -Ls https://raw.githubusercontent.com/c0rtex-dev/xsh-tunnel/main/xsh-tunnel.sh) uninstall
```

## پشتیبانی 

### برای سوالات، مشکلات یا پیشنهادات، لطفاً یک Issue در این مخزن ایجاد کنید. 